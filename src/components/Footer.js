import React from 'react'
import { fontSpacemono } from './Foonts'

const Footer = () => {
    return (
        <div className={`${fontSpacemono.className} mt-24 bg-gradient-to-r from-white/20 via-white/30 to-white/5 py-1 text-center`}>
            <h1>Created By❤️Anangs.</h1>
        </div>
    )
}

export default Footer